# Interfaces

Contratos partilhados entre camadas: `IRepository<T>`, interfaces de
repositório específicas (`IClienteRepository`, `IFornecedorRepository`, ...)
e interfaces de serviços de domínio que não dependem de infraestrutura.
