<UserControl x:Class="FinancePro.UI.Views.NotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource FP.Page}">
    <Grid Margin="26,22,26,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="18"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="18"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="FinancePro  ›  Controlo  ›  Notificações" Foreground="{DynamicResource FP.TextMuted}" FontSize="11"/>
            <TextBlock Text="Central de alertas e notificações" Foreground="{DynamicResource FP.Text}" FontSize="25" FontWeight="SemiBold" Margin="0,8,0,0"/>
            <TextBlock Text="Pendências críticas e avisos operacionais consolidados numa única área." Foreground="{DynamicResource FP.TextMuted}" FontSize="12" Margin="0,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource FP.Card}" Margin="0,0,10,0">
                <StackPanel><TextBlock Text="CRÍTICOS" Foreground="{DynamicResource PerigoBrush}" FontSize="11" FontWeight="SemiBold"/><TextBlock Text="{Binding Criticos}" Foreground="{DynamicResource FP.Text}" FontSize="26" FontWeight="SemiBold" Margin="0,6,0,0"/></StackPanel>
            </Border>
            <Border Grid.Column="1" Style="{StaticResource FP.Card}" Margin="0,0,10,0">
                <StackPanel><TextBlock Text="AVISOS" Foreground="{DynamicResource AvisoBrush}" FontSize="11" FontWeight="SemiBold"/><TextBlock Text="{Binding Avisos}" Foreground="{DynamicResource FP.Text}" FontSize="26" FontWeight="SemiBold" Margin="0,6,0,0"/></StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource FP.Card}" Margin="0,0,10,0">
                <StackPanel><TextBlock Text="INFORMAÇÕES" Foreground="{DynamicResource FP.Accent}" FontSize="11" FontWeight="SemiBold"/><TextBlock Text="{Binding Informacoes}" Foreground="{DynamicResource FP.Text}" FontSize="26" FontWeight="SemiBold" Margin="0,6,0,0"/></StackPanel>
            </Border>
            <Border Grid.Column="3" Style="{StaticResource FP.Card}">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Margin="0,0,16,0"><TextBlock Text="{Binding Mensagem}" Foreground="{DynamicResource FP.Text}" FontSize="12"/><TextBlock Text="{Binding UltimaAtualizacao, StringFormat=Atualizado: {0}}" Foreground="{DynamicResource FP.TextMuted}" FontSize="10" Margin="0,4,0,0"/></StackPanel>
                    <Button Content="Atualizar" Command="{Binding AtualizarCommand}" Style="{StaticResource FP.SecondaryButton}"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="4" Style="{StaticResource FP.Card}">
            <Grid>
                <DataGrid ItemsSource="{Binding Alertas}" Style="{StaticResource FP.DataGrid}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Severidade" Binding="{Binding Severidade}" Width="Auto"/>
                        <DataGridTextColumn Header="Mensagem" Binding="{Binding Mensagem}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
                <TextBlock Text="A carregar alertas..." Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource FP.TextMuted}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
