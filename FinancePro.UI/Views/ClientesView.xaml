<UserControl x:Class="FinancePro.UI.Views.ClientesView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Background="{StaticResource FP.Page}">
 <Grid Margin="26,22">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="18"/><RowDefinition Height="Auto"/><RowDefinition Height="18"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <Grid>
   <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
   <StackPanel><TextBlock Text="Clientes" FontSize="25" FontWeight="SemiBold" Foreground="{StaticResource FP.Text}"/><TextBlock Text="Cadastro independente para vendas, receitas e contas a receber" Foreground="{StaticResource FP.TextMuted}" Margin="0,5,0,0"/></StackPanel>
   <StackPanel Grid.Column="1" Orientation="Horizontal"><Button Content="+ Novo" Command="{Binding NovoCommand}" Style="{StaticResource FP.PrimaryButton}" Margin="0,0,10,0"/><Button Content="Atualizar" Command="{Binding AtualizarCommand}" Style="{StaticResource FP.SecondaryButton}"/></StackPanel>
  </Grid>
  <Border Grid.Row="2" Style="{StaticResource FP.Card}" Padding="16">
   <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition Width="120"/></Grid.ColumnDefinitions>
    <StackPanel><TextBlock Text="Pesquisar" Style="{StaticResource FP.FieldLabel}"/><TextBox Text="{Binding Pesquisa, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FP.FilterTextBox}"/></StackPanel>
    <Button Grid.Column="2" Content="Pesquisar" Command="{Binding AtualizarCommand}" Style="{StaticResource FP.PrimaryButton}" VerticalAlignment="Bottom"/>
   </Grid>
  </Border>
  <Grid Grid.Row="4"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="16"/><ColumnDefinition Width="360"/></Grid.ColumnDefinitions>
   <Border Style="{StaticResource FP.Card}" Padding="0">
    <DataGrid ItemsSource="{Binding Itens}" SelectedItem="{Binding Selecionado}" Style="{StaticResource FP.DataGrid}">
     <DataGrid.Columns>
      <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="2*"/><DataGridTextColumn Header="NIF" Binding="{Binding NIF}" Width="*"/><DataGridTextColumn Header="Telefone" Binding="{Binding Telefone}" Width="*"/><DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*"/>
      <DataGridTemplateColumn Header="Estado" Width="100"><DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding Ativo, Converter={StaticResource AtivoParaTexto}}" Foreground="{Binding Ativo, Converter={StaticResource AtivoParaCor}}"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
      <DataGridTemplateColumn Header="Ações" Width="100"><DataGridTemplateColumn.CellTemplate><DataTemplate><StackPanel Orientation="Horizontal"><Button Content="&#xE70F;" FontFamily="Segoe MDL2 Assets" Command="{Binding DataContext.EditarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource FP.IconButton}"/><Button Content="&#xE8D8;" FontFamily="Segoe MDL2 Assets" Command="{Binding DataContext.AlternarAtivoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource FP.IconButton}" Margin="6,0,0,0"/></StackPanel></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
     </DataGrid.Columns>
    </DataGrid>
   </Border>
   <Border Grid.Column="2" Style="{StaticResource FP.Card}" Padding="20">
    <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel>
     <TextBlock Text="Dados do Cliente" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource FP.Text}" Margin="0,0,0,18"/>
     <TextBlock Text="Nome" Style="{StaticResource FP.FieldLabel}"/><TextBox Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
     <TextBlock Text="NIF" Style="{StaticResource FP.FieldLabel}"/><TextBox Text="{Binding NIF}" Margin="0,0,0,12"/>
     <TextBlock Text="Telefone" Style="{StaticResource FP.FieldLabel}"/><TextBox Text="{Binding Telefone}" Margin="0,0,0,12"/>
     <TextBlock Text="Email" Style="{StaticResource FP.FieldLabel}"/><TextBox Text="{Binding Email}" Margin="0,0,0,12"/>
     <TextBlock Text="Morada" Style="{StaticResource FP.FieldLabel}"/><TextBox Text="{Binding Morada}" Height="70" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,0,0,14"/>
     <TextBlock Text="{Binding Mensagem}" Foreground="{StaticResource FP.Accent}" TextWrapping="Wrap" Margin="0,0,0,12"/>
     <Button Content="Guardar Cliente" Command="{Binding GuardarCommand}" Style="{StaticResource FP.PrimaryButton}" HorizontalAlignment="Stretch"/><Button Content="Carregar selecionado" Command="{Binding EditarCommand}" Style="{StaticResource FP.SecondaryButton}" HorizontalAlignment="Stretch" Margin="0,10,0,0"/>
    </StackPanel></ScrollViewer>
   </Border>
  </Grid>
 </Grid>
</UserControl>
