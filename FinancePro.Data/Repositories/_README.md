# Repositories

Implementações concretas dos repositórios definidos em
`FinancePro.Core.Interfaces` (ex.: `ClienteRepository : IClienteRepository`),
que usam o `FinanceProDbContext` para aceder aos dados.
