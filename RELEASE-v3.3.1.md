# FinancePro v3.3.1 — Estabilização do UI Framework

## Correções

- Adicionado o recurso global `FP.PageBackground`.
- Adicionados aliases de estilos `FP.PageTitle` e `FP.PageSubtitle`.
- Validada a disponibilidade de todos os recursos `StaticResource` e `DynamicResource` usados nas Views WPF.
- Preservada a ordem de carregamento dos dicionários em `App.xaml`.
- Eliminada a falha de inicialização da `ParceirosView` causada por recursos inexistentes.

## Base de dados

- Nenhuma migration necessária.
