# FinancePro ERP v2.0.2 — Engineering Foundation

## Incluído

- testes unitários da camada Application;
- execução automática de testes no GitHub Actions;
- upload de resultados e cobertura;
- `Directory.Build.props` e `global.json`;
- licença proprietária;
- guias de contribuição, segurança, conduta, branches e CI/CD;
- template de configuração de Issues;
- pacote com pasta raiz única `FinancePro`.

## Branch

Esta versão deve ser aplicada em `feature/foundation` e integrada em `develop` por Pull Request.

## Envio para o GitHub

Na branch `feature/foundation`, execute `./Commit-Foundation.ps1` depois de validar o pacote no seu computador.
